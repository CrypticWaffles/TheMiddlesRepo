<div class="container">
    <h2>Voting</h2>
    <button onclick="vote(true)" class="btn btn-success">Yes</button>
    <button onclick="vote(false)" class="btn btn-danger">No</button>
</div>
<script>
    async function vote(choice) {
      console.log('Submitting vote... Choice:', choice);

// Make a POST request to the server
  let response = await fetch('/vote', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      VideoID: 1, choice, zipId: 'Zip'
    })
  });

  // Handle the response
  if (response.ok) {
    console.log('Vote submitted successfully');
    window.location.href = '/gp/feedback';
  } else {
    console.error('Vote submission failed:', response.status, response.statusText);
    alert('Failed to submit vote');
  }
}
</script>
